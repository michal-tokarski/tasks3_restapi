
# A. DATABASE CONFIGURATION
# --------------------------

# // MySQL database on local host
# spring.jpa.hibernate.ddl-auto = update
# spring.jpa.database = mysql
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# spring.datasource.url = jdbc:mysql://localhost:3306/task_crud?serverTimezone=Europe/Warsaw&useSSL = False&allowPublicKeyRetrieval=true
# # spring.datasource.url = jdbc:mysql://localhost:8080/task_crud?serverTimezone=Europe/Warsaw&useSSL = False
# spring.datasource.username = kodilla_user2
# spring.datasource.password = kodilla_password2

# // Heroku PostgreSQL database on Heroku server
spring.jpa.hibernate.ddl-auto = update
spring.jpa.database = postgresql
# spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
# spring.jpa.database-platform = org.hibernate.dialect.PostgreSQL9Dialect
spring.datasource.url = ${JDBC_DATABASE_URL}
spring.datasource.username = ${JDBC_DATABASE_USERNAME}
spring.datasource.password = ${JDBC_DATABASE_PASSWORD}

